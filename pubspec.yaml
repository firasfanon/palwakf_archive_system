name: palwakf_evidence_archive_spatial_explorer
description: Governed local-development module for PalWakf Evidence Archive and Spatial Explorer.
publish_to: "none"
version: 0.5.0+4

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
