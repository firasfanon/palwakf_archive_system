# Public Home Compile + Upload Queue Retention Repair R2

Run from the extracted package folder:

```powershell
Set-ExecutionPolicy -Scope Process Bypass -Force
.\APPLY_PUBLIC_HOME_R2_FORCE.ps1 -ProjectRoot "C:\Users\DELL\StudioProjects\archive_system"
```

Then run the normal gates from the project root.
